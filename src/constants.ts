/** Smallest selectable clip, in seconds. Shared by the timeline drag and the
 * I/O keyboard shortcuts. Handles point outward (see App.css) so they never
 * overlap, making this a usability floor rather than a grabbability constraint. */
export const MIN_SELECTION = 1;
