# Shell scripts must stay LF or they break on macOS/Linux CI runners.
*.sh text eol=lf

# Normalize text files, let git detect the rest.
* text=auto

# Binary assets must never get EOL normalization.
*.woff2 binary
*.woff binary
*.png binary
*.ico binary
*.icns binary
